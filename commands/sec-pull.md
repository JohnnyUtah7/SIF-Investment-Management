---
name: sec-pull
description: Pull SEC filings and XBRL financials for a ticker
---

# SEC pull

1. Check `EDGAR_IDENTITY`
2. Run `company-intake` if needed
3. Run `sec-filings` (prefer `scripts/edgar_pull.py`)
